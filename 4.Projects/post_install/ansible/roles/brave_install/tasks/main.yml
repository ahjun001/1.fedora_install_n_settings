---
# tasks to install brave

- name: Install dnf-plugins-core
  ansible.builtin.dnf:
    name: dnf-plugins-core
    state: present

- name: Add brave-browser repository
  yum_repository:
    name: brave-browser
    description: Brave Browser
    baseurl: https://brave-browser-rpm-release.s3.brave.com/$basearch
    enabled: yes

- name: Import Brave browser GPG key
  rpm_key:
    key: https://brave-browser-rpm-release.s3.brave.com/brave-core.asc
    state: present

- name: Tweak brave-browser.repo
  ansible.builtin.lineinfile:
    path: /etc/yum.repos.d/brave-browser.repo
    line: "{{ item }}"
    insertbefore: "^$"
    state: present
  with_items:
    - autorefresh = 1
  become: true

- name: Install Brave browser
  ansible.builtin.dnf:
    name: brave-browser
    state: latest
#
# - name: Install dnf-plugins-core
#   ansible.builtin.dnf:
#     name: dnf-plugins-core
#     state: present

# - name: Add Brave browser repository
#   ansible.builtin.shell: sudo dnf config-manager --add-repo https://brave-browser-rpm-release.s3.brave.com/brave-browser.repo

# - name: Import Brave browser GPG key
#   ansible.builtin.shell: sudo rpm --import https://brave-browser-rpm-release.s3.brave.com/brave-core.asc

# - name: Install Brave browser
#   ansible.builtin.dnf:
#     name: brave-browser
#     state: latest
