---
# playbook.yml
- hosts: all
  become: true
  become_method: sudo
  roles:
    - "config_dnf"
    - "update_all_packages"
    - "dnf_many_install"
    # - "ibus_chinese_ime"
    - "vim_install"
    - "nvim_install"
    - "vscode_install"
    - "chrome_install"
    - "brave_install"
