---
# vars/vm_images.d/f43-kde.yml
# Workflow manifest for f43-kde VM

f43_kde_image_config:
  base_image: f43-kde-base.qcow2   # immutable golden image (KDE Spin)
  current_stage: setup             # current workflow stage: setup, installed, provisioned, upgraded
  image_pool: default              # libvirt pool
  image_path: /var/lib/libvirt/images
  artifacts:
    - name: f43-kde.setup-20251112.qcow2
      stage: setup
      backing_image: f43-kde-base.qcow2
      is_full_image: false
      created: null
      description: "Clean install, ready for provisioning"
    - name: f43-kde.installed-20251112.qcow2
      stage: installed
      backing_image: null
      is_full_image: true
      created: null
      description: "OS installed and validated, full independent image"
    - name: f43-kde.provisioned-v1.qcow2
      stage: provisioned
      backing_image: f43-kde.installed-20251112.qcow2
      is_full_image: false
      created: null
      description: "Provisioned with base tools and configs"
